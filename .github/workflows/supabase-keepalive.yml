# Keeps your Supabase free-tier project active so it never auto-pauses
# from inactivity (Supabase pauses free projects after 7 days with no API activity).
#
# Setup:
#   1. Save this file as .github/workflows/supabase-keepalive.yml in any GitHub repo
#   2. Push it — GitHub Actions will run it on the schedule below automatically
#
# Generated by SupaMigrate — https://supamigrate.pages.dev/supabase-keep-alive-tool

name: Supabase Keep-Alive

on:
  schedule:
    - cron: "0 0 */3 * *"
  workflow_dispatch: {} # allows manual "Run workflow" from the Actions tab

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase REST API
        run: |
          curl -sS -o /dev/null -w "HTTP %{http_code}\n" \
            "https://ogningqqgxhwkmozikmu.supabase.co/rest/v1/" \
            -H "apikey: sb_publishable_Q9SEFHhKlsG05lL4dmrSqw_hwbvmEAB" \
            -H "Authorization: Bearer sb_publishable_Q9SEFHhKlsG05lL4dmrSqw_hwbvmEAB"
