/**
 * About page entry: only needs the shared theme toggle wiring.
 * The page has no sightings, filter panel, or burger menu.
 */

import { bindAppearanceMode } from "../browse/index.js";

bindAppearanceMode();
